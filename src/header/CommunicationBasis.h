#include <iostream>
#include <string>
#include <vector>
#include <unordered_map>

//communication will be a list of token, just like ai splitting
const std::vector<std::string> harsh_token = {
}
struct TOKEN{
  std::string token_split;
};
